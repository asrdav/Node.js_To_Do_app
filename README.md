A to-do web app made of node.js in which one can add/delete items just by one click.


(node_modules folder is not complete. I will upload all the dependencies later. Else all other working code is present.)
